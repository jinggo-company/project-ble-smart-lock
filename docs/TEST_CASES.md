# TEST_CASES — BLE 智能锁固件开发与 App 对接

## 项目信息

| 字段 | 值 |
|------|------|
| 项目 ID | P-2026-00011 |
| 关联任务 | T-2026-00044, T-2026-00052, T-2026-00053 |

---

## 测试案例清单

### TC-001: BLE 配对与连接

| Case-ID | 测试项 | 步骤 | 预期 | AC 映射 |
|---------|--------|------|------|---------|
| TC-001-01 | LESC 配对成功 | App 发起配对 → 输入 PIN → 验证 | 配对成功，会话密钥生成 | 加密通信 |
| TC-001-02 | 配对超时 | 30s 内未完成配对 | 配对失败，回到可配对状态 | 连接管理 |
| TC-001-03 | 拒绝配对 | 锁控板拒绝配对请求 | App 收到拒绝响应 | 安全 |
| TC-001-04 | 已配对设备重连 | 断开后重连 | 自动重连，无需重新配对 | 连接管理 |
| TC-001-05 | 同时连接数超限 | 3 个设备同时尝试连接 | 第 3 个被拒绝 | 连接管理 |

### TC-002: 加密通信

| Case-ID | 测试项 | 步骤 | 预期 | AC 映射 |
|---------|--------|------|------|---------|
| TC-002-01 | 开锁指令加密传输 | App 发送加密开锁指令 | 锁控板解密成功并执行开锁 | 加密通信 |
| TC-002-02 | 未加密消息拒绝 | 发送明文指令到 0xF002 | 锁控板丢弃，不响应 | 安全 |
| TC-002-03 | HMAC 验证失败 | 篡改 HMAC 字段 | 锁控板拒绝，返回错误码 | 安全 |
| TC-002-04 | Nonce 重复检测 | 发送相同 Nonce 的消息 | 锁控板拒绝（重放攻击防护） | 防重放 |
| TC-002-05 | 时间戳窗口过期 | 发送时间戳超 ±5s 的消息 | 锁控板拒绝 | 防重放 |
| TC-002-06 | ECDH 密钥协商 | App 和锁控板交换公钥 | 双方计算出相同会话密钥 | 密钥交换 |

### TC-003: 开锁功能

| Case-ID | 测试项 | 步骤 | 预期 | AC 映射 |
|---------|--------|------|------|---------|
| TC-003-01 | 正常开锁 | 发送开锁指令 | 电机转动 3s → 锁舌收回 → 返回成功 | AC-1 |
| TC-003-02 | 电机卡阻检测 | 开锁时电机堵转 | 检测到异常电流 → 停止电机 → 返回错误 | AC-1 |
| TC-003-03 | 锁舌到位检测 | 开锁后 Hall 传感器变化 | 确认锁舌完全收回 | AC-1 |
| TC-003-04 | 自动上锁 | 开锁 5s 后无操作 | 电机反向转动 → 锁舌伸出 → 上锁 | AC-1 |
| TC-003-05 | 低电量开锁拒绝 | 电量 < 5% | 拒绝开锁，返回低电量错误 | AC-1 |
| TC-003-06 | 连续开锁 3 次失败 | 3 次错误指令 | 锁定 5 分钟，触发蜂鸣告警 | 安全 |

### TC-004: 密钥管理

| Case-ID | 测试项 | 步骤 | 预期 | AC 映射 |
|---------|--------|------|------|---------|
| TC-004-01 | 添加管理员密钥 | App 发送密钥添加指令 | 锁控板存储密钥，返回成功 | 密钥管理 |
| TC-004-02 | 添加临时密钥（限时） | 设置 24h 有效期的密钥 | 到期后自动失效 | 密钥管理 |
| TC-004-03 | 撤销密钥 | App 发送密钥撤销指令 | 密钥立即失效，开锁被拒绝 | 密钥管理 |
| TC-004-04 | 密钥列表查询 | 读取密钥列表 | 返回所有有效密钥 | 密钥管理 |
| TC-004-05 | 密钥存储上限 | 存储 100 个密钥后继续添加 | 返回存储空间不足错误 | 密钥管理 |

### TC-005: 状态通知

| Case-ID | 测试项 | 步骤 | 预期 | AC 映射 |
|---------|--------|------|------|---------|
| TC-005-01 | 锁状态 Notify | 锁舌状态变化 | App 收到状态变化通知 | 状态同步 |
| TC-005-02 | 电池电量 Notify | 电量低于 20% | App 收到低电量通知 | 状态同步 |
| TC-005-03 | 防拆告警 | 打开锁体后盖 | App 收到防拆告警通知 | 安全 |
| TC-005-04 | 固件版本查询 | 读取设备信息特征值 | 返回当前固件版本号 | 设备信息 |

### TC-006: OTA 升级

| Case-ID | 测试项 | 步骤 | 预期 | AC 映射 |
|---------|--------|------|------|---------|
| TC-006-01 | 完整 OTA 流程 | 传输完整固件包 → 验证签名 → 重启 | 升级成功，返回新版本号 | OTA |
| TC-006-02 | OTA 中断恢复 | 传输 50% 时断开 → 重连 | 支持断点续传 | OTA |
| TC-006-03 | OTA 签名验证失败 | 传输被篡改的固件包 | 拒绝升级，回退到旧版本 | OTA 安全 |
| TC-006-04 | OTA 低电量拒绝 | 电量 < 30% 时发起 OTA | 拒绝升级，提示充电 | OTA |
| TC-006-05 | 版本回退保护 | 尝试刷入旧版本固件 | 拒绝降级（除非强制模式） | OTA |

### TC-007: 低功耗

| Case-ID | 测试项 | 步骤 | 预期 | AC 映射 |
|---------|--------|------|------|---------|
| TC-007-01 | 休眠电流 | 无连接，仅广播 | 电流 ≤ 1mA | 低功耗 |
| TC-007-02 | 连接电流 | BLE 连接保持 | 电流 ≤ 15mA | 低功耗 |
| TC-007-03 | 休眠唤醒 | 触摸电容唤醒 → 开始广播 | 唤醒时间 ≤ 500ms | 低功耗 |
| TC-007-04 | 待机续航 | 满电，每天 10 次开锁 | 续航 ≥ 12 个月 | 低功耗 |

### TC-008: 日志

| Case-ID | 测试项 | 步骤 | 预期 | AC 映射 |
|---------|--------|------|------|---------|
| TC-008-01 | 开锁日志记录 | 每次开锁/上锁 | 日志写入 Flash | 日志 |
| TC-008-02 | 日志读取 | App 读取最近 100 条日志 | 按时间倒序返回 | 日志 |
| TC-008-03 | 日志溢出处理 | 日志存储满后继续记录 | 环形缓冲覆盖最旧记录 | 日志 |
| TC-008-04 | 日志同步到后端 | App 读取日志并上传 | 后端存储日志记录 | 日志 |

### TC-009: App 端测试

| Case-ID | 测试项 | 步骤 | 预期 | AC 映射 |
|---------|--------|------|------|---------|
| TC-009-01 | App 扫描设备 | 打开 App → 扫描周围 BLE 设备 | 显示可配对的智能锁 | App 功能 |
| TC-009-02 | App 开锁 UI | 点击开锁按钮 | 发送指令 → 显示开锁中 → 成功/失败 | App 功能 |
| TC-009-03 | App 断线重连 | 断开连接后 | 自动重连，指数退避（1s→2s→4s→8s） | App 功能 |
| TC-009-04 | App OTA 升级 | 点击升级 → 传输进度 → 完成 | 显示升级进度条，完成后提示重启 | App 功能 |
| TC-009-05 | App 密钥分享 | 分享密钥给其他用户 | 生成临时分享链接 | App 功能 |

### TC-010: 后端 API 测试

| Case-ID | 测试项 | 步骤 | 预期 | AC 映射 |
|---------|--------|------|------|---------|
| TC-010-01 | 用户注册登录 | POST /api/auth/register | JWT 返回 | API |
| TC-010-02 | 设备绑定 | POST /api/devices/bind | 设备绑定到用户 | API |
| TC-010-03 | 远程开锁 | POST /api/devices/:id/unlock | 开锁成功（需网关） | API |
| TC-010-04 | 密钥管理 API | CRUD 密钥操作 | 密钥正确分发/撤销 | API |
| TC-010-05 | 日志查询 API | GET /api/devices/:id/logs | 分页返回日志 | API |
| TC-010-06 | JWT 鉴权失败 | 无效 Token 访问 API | 401 Unauthorized | API 安全 |

---

## 测试环境要求

| 环境 | 要求 |
|------|------|
| 硬件 | nRF52 DK 开发板 × 2, 电机模拟器, Hall 传感器模块 |
| 软件 | nRF Connect SDK 2.5+, nRF Desktop Control, J-Link |
| App | iOS 15+ / Android 10+ 真机或模拟器 |
| 后端 | Docker Desktop, PostgreSQL 16, Redis 7 |
| 抓包 | Wireshark + nRF Sniffer for BLE |

## 测试执行顺序

1. TC-001（BLE 配对）→ 2. TC-002（加密通信）→ 3. TC-003（开锁功能）
4. TC-004（密钥管理）→ 5. TC-005（状态通知）→ 6. TC-006（OTA）
7. TC-007（低功耗）→ 8. TC-008（日志）→ 9. TC-009（App 功能）
10. TC-010（后端 API）
